#include "klib.h"

int main() {


/*
    uint64_t pre_time = read_timer();
    uint64_t cur_time ;
    int i = 0;

    while(1) {
      cur_time = read_timer();
      if( (cur_time - pre_time) >= 50000ull ) {
        pre_time = cur_time ;
        i++;
        printf("at time: %02d ms\n", i);
        if( i == 10)  return 0;
      }
    }

    return 0;
}
*/

/*
  printf("Hello, World!\n");
*/

  return 0;
}

